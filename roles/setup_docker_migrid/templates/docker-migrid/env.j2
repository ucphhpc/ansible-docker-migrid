# Templated by Ansible. Do not modify this directly.
# See group_vars and host_vars in the common Ansible repository to make changes.
{% if docker_migrid is defined %}
{## Sort by key ##}
{% for key, value in docker_migrid | dictsort %}
{% if docker_migrid[key] is string %}
{{ key.upper() }}={{ docker_migrid[key]|to_json }}
{% else %}
{{ key.upper() }}={{ docker_migrid[key] }}
{% endif %}
{% endfor %}
{% else %}
# No docker_migrid defined
{% endif %}
